 public static Comparator<Page> pRank = (Page p1, Page p2) -> {
        int pRank1 = p1.getRank();
        int pRank2 = p2.getRank();
        return pRank2-pRank1;
    };


Collections.sort(p,Page.pRank); //Method to be called in Main.
